#!/usr/bin/env python3
"""Predicts something idk what but definitely does something."""

import tensorflow.keras as K


def predict(network, data, verbose=False):
    """just look at what this returns duhhh."""
    return network.predict(data, verbose=verbose)
